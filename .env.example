# Django Settings
DEBUG=True
SECRET_KEY=your-secret-key-here-change-in-production
ALLOWED_HOSTS=localhost,127.0.0.1

# Database Settings
# USE_SQLITE=True  -> Gunakan SQLite untuk development lokal (tanpa Docker)
# USE_SQLITE=False -> Gunakan MySQL untuk production (dengan Docker)
USE_SQLITE=True

# MySQL Settings (abaikan jika USE_SQLITE=True)
DB_NAME=cctv_pontianak
DB_USER=dishub_admin
DB_PASSWORD=your-secure-password-here
DB_HOST=db
DB_PORT=3306

# Admin User (for initial setup)
DJANGO_SUPERUSER_USERNAME=admin
DJANGO_SUPERUSER_EMAIL=admin@pontianak.go.id
DJANGO_SUPERUSER_PASSWORD=admin123changeme
